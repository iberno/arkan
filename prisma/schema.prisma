// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite" // Pode ser alterado para "postgresql", "mysql", etc.
  url      = env("DATABASE_URL")
}
